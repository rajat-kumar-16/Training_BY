// interface class has only abstract methods, also by default it has only public abstract method
//no constructor, so assign the values to variable// multiple inheritance
//class and interface can implements multiple interface
// also if two interfaces have same method than both methods will be same for some child class.